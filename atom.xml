<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>JaisonG1n</title>
  <subtitle>Code, AI and Everything in Between</subtitle>
  <link href="https://jaisong1n.com/" rel="alternate" type="text/html"/>
  <link href="https://jaisong1n.com/atom.xml" rel="self" type="application/atom+xml"/>
  <id>https://jaisong1n.com/</id>
  <updated>2026-08-25T07:37:02.000Z</updated>
  <language>zh_CN</language>
  <entry>
    <title>只让模型处理不确定性：一次 Agent 工具调用与上下文优化复盘</title>
    <link href="https://jaisong1n.com/posts/agent-tool-calling-context-optimization/" rel="alternate" type="text/html"/>
    <id>https://jaisong1n.com/posts/agent-tool-calling-context-optimization/</id>
    <published>2026-08-25T07:37:02.000Z</published>
    <updated>2026-08-25T07:37:02.000Z</updated>
    <summary>在一个基于 Spring AI 的微信助手里，我把确定的业务流程、会话状态和高置信路由逐步移出模型循环。本文复盘这次边界重划：如何减少无意义的模型调用、控制 Tool Schema 和历史上下文，并让 Agent 更稳定、更容易维护。</summary>
    <content type="html"><![CDATA[<p>最近我在做一个基于 Spring AI 的微信智能助手。项目早期的思路很直接：接入大模型，把天气、健康档案、冰箱库存、菜谱、营养计算、运动计划、提醒等能力包装成 <code>@Tool</code>，再交给模型自己选择。</p>
<p>刚开始，这种开发体验确实很顺。写一个 Service，外面包一层 Tool，注册到 Spring AI，模型马上就多了一项能力。我当时很自然地认为：Tool 越多，Agent 就越强。</p>
<p>但随着工具增长到十几个、业务流程开始跨越多个领域，问题逐渐出现：响应变慢，输入变长，工具调用顺序不稳定，一些只读查询会被重复执行，排查问题时也很难判断到底是模型决策、上下文还是业务代码出了偏差。</p>
<p>这轮重构之后，我最大的认识是：</p>
<blockquote>
<p><strong>Agent 工程真正难的地方，不是让模型拥有更多能力，而是判断什么时候根本不该让模型参与。</strong></p>
</blockquote>
<h2>从自由 Tool Loop 开始</h2>
<p>项目早期的主链路很典型：</p>
<pre><code>用户消息
→ ChatClient
→ 模型读取 Prompt、历史与全部 Tool Schema
→ 模型选择 Tool
→ Tool 执行并返回结果
→ 模型继续决策或组织最终回复
</code></pre>
<p>对于“今天杭州天气怎么样”这样的单步问题，这套流程没有明显问题。模型选中天气工具，取得数据，再组织一句自然语言回答。</p>
<p>但“今天三个人，预算 80 元，帮我安排一顿晚饭”完全不同。它背后可能需要读取健康档案和库存，生成菜谱，计算食材需求与营养，生成采购清单，最后校验预算。如果把这些步骤全部交给模型自由编排，执行过程就会变成反复的 <code>LLM → Tool → LLM</code>。</p>
<p>从演示效果看，这很有 Agent 的味道；从工程角度看，它却把一个已知流程重新变成了概率问题。调用顺序可能变化，某一步可能遗漏，同一个只读工具可能重复执行，模型也可能在拿到中间结果后重新规划整条链路。</p>
<p>一旦流程已经足够确定，让模型继续负责流程编排，通常不会增加价值，只会增加延迟、Token 消耗和不确定性。</p>
<h2>Tool 不是免费的能力列表</h2>
<p>过去谈 Token 优化，我首先想到的是聊天历史。但一次 Agent 请求里，模型看到的不只是用户的当前消息，还可能包括 System Prompt、Tool Definitions、参数 JSON Schema、Memory、RAG 结果、历史消息和上一轮 Tool Result。</p>
<p>Java 里一个看似简单的方法：</p>
<pre><code>@Tool(description = "查询用户冰箱库存")
public PantryResult getPantry(...) {
    ...
}
</code></pre>
<p>到了模型侧，会展开成工具名称、描述、参数类型、字段说明和必填约束。三个工具时，这部分成本很小；当工具越来越多时，即使用户只问“今天几号”，请求中也可能携带菜谱、库存、营养、运动和提醒等完全无关的 Schema。</p>
<p>工具数量因此同时影响四件事：输入 Token、Prefill 时间、候选选择难度，以及误调用机会。给 Agent 注册所有能力对开发者很方便，却不一定是合理的运行时设计。</p>
<h2>优化顺序应该从“能否不调用模型”开始</h2>
<p>一开始我也想过继续压缩 System Prompt。但把一段 Prompt 从 1000 Token 缩短到 800 Token，收益往往不如直接消除一次没有必要的模型请求。</p>
<p>后来我的优化顺序逐渐变成：</p>
<pre><code>1. 这一步能不能不用模型？
2. 能不能减少一次模型调用？
3. 能不能缩小 Tool 候选集合？
4. 能不能只加载当前任务相关的上下文？
5. Tool Result 能不能只保留必要字段？
6. RAG 和 Memory 能不能按相关性限量？
7. 最后再压缩 Prompt 文案。
</code></pre>
<p>这个顺序改变了问题的性质。Token 优化不再只是 Prompt Engineering，而变成了模型调度和系统边界设计。</p>
<h2>把确定流程移出 Agent Tool Loop</h2>
<p>项目里后来引入了 <code>BusinessSkill</code>。它的作用很简单：一个业务如果已有明确步骤，就由 Java 代码稳定编排，不再让模型每次现场决定下一步。</p>
<p>以晚餐规划为例，原先可能由模型自由选择健康档案、库存、菜谱、营养和采购工具；重构后，<code>DinnerBudgetPlanningSkill</code> 直接调用对应的领域服务，确定执行顺序、参数校验、预算判断和状态变化。</p>
<p>模型只保留它真正擅长的部分：</p>
<ul>
<li>理解用户想完成什么；</li>
<li>从自然语言中抽取参数；</li>
<li>判断表达是否存在歧义；</li>
<li>把结构化结果解释得自然、清楚。</li>
</ul>
<p>而业务系统负责调用顺序、数据读写、事务、幂等、权限与确认。对这些已经确定的逻辑来说，模型的随机性不是能力，而是额外风险。</p>
<h2>不是每一句话都值得问模型</h2>
<p><code>BusinessSkill</code> 解决了执行阶段的确定性，路由阶段还可以继续前移。项目现在的处理思路可以概括为：</p>
<pre><code>Control Command
→ ExpectedInput
→ RuleBased Router
→ Intent Model Router
→ 其他兜底路径
</code></pre>
<p>原则是：越确定的问题，越靠前用本地代码处理。</p>
<h3>控制指令：零次模型调用</h3>
<p>如果系统正等待用户确认运动记录，那么“确认记录运动”“取消”“继续”这类回复的含义已经由会话状态限定，没有必要再让模型解释一次。本地状态机可以直接处理。</p>
<h3>ExpectedInput：系统应该记得自己刚问过什么</h3>
<p>假设机器人上一轮问“预算是多少”，会话里已经记录 <code>expectedField = budget</code> 和字段类型。用户回复“100”“一百块”或“预算一百左右”时，本地解析器可以直接填充参数。</p>
<p>如果系统明明知道自己刚问了什么，却还把用户回答连同一大段历史发给模型重新推理，这是一种很隐蔽的浪费。</p>
<h3>规则路由：高置信表达直接命中</h3>
<p>“查看运动记录”“查看健康档案”“今天几号”这类边界清晰的表达，可以由规则直接路由。只有“最近想稍微练一练，但又不想太累”这种包含目标、程度和隐含约束的表达，才值得进入意图模型。</p>
<p>我现在更愿意把 LLM 看成歧义处理器，而不是所有请求的默认入口。</p>
<h2>Context 不等于最近 N 条聊天记录</h2>
<p>项目早期会把最近一段聊天历史直接交给模型。实现简单，但微信只有一个聊天窗口：用户可能先问天气，再规划运动，接着聊晚餐，之后又转到内容创作。“最近 20 条消息”与“当前任务需要的信息”很快就不再是同一件事。</p>
<p>无关历史不只消耗 Token，还会污染当前任务。模型可能把上一次任务的预算、人数或偏好误带到这一次。</p>
<p>因此，我现在把上下文理解为：</p>
<pre><code>当前目标
+ 当前任务状态
+ 已确认约束
+ 当前缺失参数
+ 最近真正相关的消息
</code></pre>
<p>聊天历史只是 Context 的一种原材料，而不是 Context 本身。</p>
<h2>用 SkillSession 保存状态，而不是让模型反复回忆</h2>
<p>以晚餐规划为例，与其让模型每轮从历史里重新寻找人数、预算、忌口和执行进度，不如把这些信息直接保存为结构化会话状态：</p>
<pre><code>skill: DinnerBudgetPlanningSkill
action: PLAN_DINNER

collectedParams:
  people: 3
  budget: 80

missingFields:
  - dietaryPreference

expectedInput:
  dietaryPreference
</code></pre>
<p>下一轮用户只说“不吃香菜”，系统已经知道当前等待的是 <code>dietaryPreference</code>。这既减少历史重放，也避免模型重新推理流程位置。</p>
<p><strong>业务状态应该存在业务系统里，而不是只存在 Prompt 里。</strong></p>
<h2>真实聊天一定会被打断</h2>
<p>用户不会按开发者设计好的顺序聊天。机器人刚问“几个人”，用户可能突然插一句“对了，今天杭州会下雨吗”。</p>
<p>项目把这类关系区分为当前话题、只读打断、新话题、混合请求和歧义请求。天气查询属于典型的只读打断：系统可以保存当前晚餐 Skill，回答天气，然后继续等待原先的人数输入。</p>
<p>这里真正帮助上下文管理的，不是更大的上下文窗口，而是 <code>ConversationRuntimeContext</code>。它明确记录当前活跃任务、暂停任务、待确认操作、话题版本和恢复位置。</p>
<p>因此，“不要无限保存 Prompt，要保存状态”不只是 Token 技巧，也是多轮交互可靠性的基础。</p>
<h2>长期上下文要压缩成可使用的结构</h2>
<p>短期任务可以保留少量相关消息，长期使用则不能无限累积。更合适的方式，是把详细历史逐步压缩为结构化 Summary：</p>
<pre><code>Goal:
  FAT_LOSS

Constraints:
  people: 3
  weeklyBudget: 300
  dislikedFood:
    - 香菜

Confirmed:
  healthProfile: true

OpenItems:
  - 下周菜单待确认
</code></pre>
<p>和一段模糊的自然语言回忆相比，结构化摘要更短、更稳定，也更容易被程序裁剪和复用。Context Engineering 真正研究的，不是怎样塞入更多内容，而是这一轮哪些信息值得进入模型。</p>
<h2>Tool Result 也要面向模型裁剪</h2>
<p>数据库实体里可能包含 ID、用户标识、创建时间、更新时间、版本号、内部状态和审计字段，但模型规划晚餐时，真正需要看到的也许只是“鸡胸肉 500g、鸡蛋 6 个、番茄 4 个”。</p>
<p>如果把完整 Entity 直接序列化回模型，所有内部字段都会变成 Token，也可能暴露模型不需要接触的实现细节。更合理的边界是：</p>
<pre><code>Domain Entity
→ 面向模型的精简 DTO
→ LLM
</code></pre>
<p>系统内部保留完整业务数据，只把完成当前推理所需的字段交给模型。数据存在，不代表模型需要知道。</p>
<h2>还没有完成的两项优化</h2>
<p>目前项目仍保留了一部分通用 Tool Calling，这里还有两个明确的后续方向。</p>
<p>第一是动态 <code>ToolBundle</code>。普通聊天可以不暴露工具，天气问题只暴露天气与日期，菜谱问题只暴露库存、菜谱和营养；真实写操作则直接进入对应 <code>BusinessSkill</code>，不交给通用 Agent 自由选择。</p>
<p>第二是按用途拆分 Prompt Profile，例如意图路由、普通聊天、工具 Agent、规划、内容生成和输出修复。意图路由只需要知道有哪些 Skill、Action 以及结构化输出约束，不需要同时背负内容创作和最终回复风格。</p>
<p>这两项目前是下一阶段设计方向，而不是已经验证完成的能力。</p>
<h2>这次重构留下的三个结论</h2>
<h3>一、Tool 越多，不等于 Agent 越强</h3>
<p>工具数量增加会扩大 Schema、候选空间和误调用机会。真正重要的是，正确的工具能否在正确的时候被暴露。</p>
<h3>二、Context 不等于 Chat History</h3>
<p>上下文更接近目标、状态、约束、决策、待补参数和相关事实的组合。历史消息只是其中一种来源。</p>
<h3>三、最省 Token 的方法，是少让模型处理确定性</h3>
<p>金额解析、单位换算、状态机、权限、数据库事务、公式、幂等判断，这些都更适合由代码稳定完成。LLM 的价值应该留给语言理解、歧义判断、信息抽取、开放生成与解释。</p>
<h2>写在最后</h2>
<p>做这个项目之前，我更关注“怎样让模型学会调用所有工具”。现在我更常问的是：“这一轮为什么要让模型看到这个工具？”</p>
<p>模型擅长处理自然语言中的不确定性，工程系统擅长状态、规则、事务、类型和确定性。两者真正配合起来以后，Agent 才不只是一个会调用 API 的聊天机器人，而开始像一个能够长期维护的业务系统。</p>
<blockquote>
<p><strong>确定性的事情尽量交给代码，不确定性的事情才值得交给模型。</strong></p>
</blockquote>
]]></content>
    <author>
      <name>JaisonG1n</name>
    </author>
    <category term="Uncategorized"></category>
  </entry>
  <entry>
    <title>Tool、Skill 与 MCP：能力、工作流与标准协议，不在同一个层次</title>
    <link href="https://jaisong1n.com/posts/tool-skill-mcp-capability-workflow-protocol/" rel="alternate" type="text/html"/>
    <id>https://jaisong1n.com/posts/tool-skill-mcp-capability-workflow-protocol/</id>
    <published>2026-08-04T03:49:37.000Z</published>
    <updated>2026-08-04T07:27:27.000Z</updated>
    <summary>Tool、Skill 与 MCP：能力、工作流与标准协议，不在同一个层次 打开任意一个 Agent 相关文档， […]</summary>
    <content type="html"><![CDATA[<h1>Tool、Skill 与 MCP：能力、工作流与标准协议，不在同一个层次</h1>
<p>打开任意一个 Agent 相关文档，你都会同时看到三个词：Tool、Skill、MCP。有人把 Skill 当成”更复杂的 Tool”，有人把 MCP 称为”插件系统”，还有人以为接入 MCP 就不需要本地工具了。这些说法不是完全错误，但都混淆了一个关键事实：<strong>三者解决的并不是同一个问题，它们位于同一个 Agent 系统的不同层次</strong>。</p>
<p>这篇文章用三层框架拆开它们：Tool 是 Agent 可以执行的<strong>原子能力</strong>，Skill 是围绕任务组织起来的<strong>可复用工作流与规则</strong>，MCP 是客户端与外部能力之间的<strong>标准连接协议</strong>。为了让讨论落地，我会结合当前 OpenClaw 博客 Agent 的真实结构——它同时包含本地插件 Tool（如 <code>generate_academic_figure</code>）与 Skill（如 <code>academic-figure-prompt</code>），目前尚未接入 MCP Server。</p>
<h2>先给结论：它们属于不同层次</h2>
<p>先把三者并排放一张表，便于后文展开：</p>
<table>
<thead>
<tr>
<th>概念</th>
<th>主要解决的问题</th>
<th>典型内容</th>
<th>是否属于协议</th>
</tr>
</thead>
<tbody>
<tr>
<td>Tool</td>
<td>让模型执行一个具体动作</td>
<td>函数、API 调用、文件操作，带结构化输入输出</td>
<td>否</td>
</tr>
<tr>
<td>Skill</td>
<td>让 Agent 按稳定流程完成一类任务</td>
<td>规则、步骤、模板、工具调用策略与安全边界</td>
<td>通常不是（各平台实现不同）</td>
</tr>
<tr>
<td>MCP</td>
<td>让客户端以标准方式连接外部能力</td>
<td>tools、resources、prompts 等服务器侧能力</td>
<td>是</td>
</tr>
</tbody>
</table>
<p>一个关键前提必须先说清：<strong>Skill 不是所有 AI 产品共同遵循的统一技术标准</strong>。OpenAI 的”tools”、Anthropic 的”tool use”、OpenClaw 的 Skill 目录、MCP 的 prompts——它们对”能力”和”工作流”的包装方式各不相同。后文凡是提到 Skill，指的是本仓库这类”以 SKILL.md 描述任务规则”的实现，不代表所有平台都相同。</p>
<p><em>图：Tool、Skill 与 MCP 的三层关系总览。上层是 Agent 与用户意图，中层是 Skill 的任务编排，下层是本地 Tool 与 MCP 标准连接两条能力供给路径；图片由 qwen-image-2.0 生成，文字与箭头细节等待人工核验。</em></p>
<h2>Tool：让模型真正执行动作</h2>
<p><img src="https://cms.jaisong1n.com/wp-content/uploads/2026/08/mcp-progressive-discovery.png" alt="MCP 工具按需发现流程" /></p>
<p>MCP 官方文档对 Tool 调用流程的示意：按需发现、检查与调用三阶段。来源：Model Context Protocol project，许可 CC BY 4.0，未修改。</p>
<p>大模型本身只能输出文本。要让它查询数据库、调用 API、读写文件，就需要把”动作”包装成模型可调用、可校验、可反馈的对象——这就是 Tool。</p>
<p>Tool 的核心特征有四条：</p>
<ol>
<li><strong>结构化输入输出</strong>：Tool 用参数 Schema 声明它接受什么、返回什么。OpenAI 的 function calling 中，每个 function tool 由 <code>name</code>、<code>description</code> 和 <code>parameters</code>（JSON Schema）定义；Anthropic 的 tool use 同样通过 <code>tools</code> 参数把函数定义交给模型，模型在需要时返回 <code>tool_use</code> 消息，系统执行后把结果作为 <code>tool_result</code> 交回。</li>
<li><strong>参数校验与权限</strong>：工具在调用前校验参数合法性，在调用时遵循权限边界，错误以结构化方式返回，而不是让模型”猜”结果。</li>
<li><strong>与自然语言回复区分</strong>：Tool 调用是”动作”，模型回复是”文本”。一个负责改变世界，一个负责说明。</li>
<li><strong>来源不限</strong>：Tool 可以由客户端进程直接注册（本地 Tool），也可以由 MCP Server 暴露——但”由 MCP 暴露”只是 Tool 的一种供给渠道，不是唯一渠道。</li>
</ol>
<p>以当前 OpenClaw 插件为例，<code>generate_academic_figure</code> 就是一个本地注册的 Tool：它声明了 <code>prompt</code>、<code>figureSpec</code>、<code>size</code> 等参数（TypeBox Schema），执行时校验参数、调用 DashScope API、下载图片并返回本地路径。简化后的结构大致是：</p>
<pre><code>tool {
  name: "generate_academic_figure",
  parameters: { prompt: string, size?: string, ... },
  execute: (params) =&gt; { 校验 → 调 API → 保存 → 返回结果 }
}
</code></pre>
<p>这个例子里还藏着 Tool 的另外两个关键设计：</p>
<ul>
<li><strong>错误处理是 Tool 的一部分</strong>。参数非法、鉴权失败、限流、超时、下载到的内容不是图片——这些都要由 Tool 转成模型能理解的错误摘要，而不是让模型凭空猜测发生了什么。本地实现里，网络错误、服务端 5xx、429 限流各有明确处理：429 与 5xx 最多退避重试一次，参数与鉴权错误不重试，超时后绝不自动重试（因为无法确认服务端是否已经成功生成）。</li>
<li><strong>Tool 是权限的边界</strong>。谁在什么条件下可以调用、调用后能碰哪些资源，都由 Tool 这一层把关。它调用百炼 API 时只携带最小必要的权限，下载图片时校验协议、内容类型与文件大小，把结果保存在受控目录——这些约束写在 Tool 代码里，而不是留给模型”自觉”。</li>
</ul>
<p><strong>Tool 不等于自然语言能力</strong>：它把”执行”从”表达”中分离出来，是 Agent 能够行动的物理基础。</p>
<h2>Skill：把工具组织成可重复的任务方法</h2>
<p><img src="https://cms.jaisong1n.com/wp-content/uploads/2026/08/output.png" alt="Skill Workflow 与 Tool 执行的分工" /></p>
<p>Skill 负责编排（Figure Spec、Prompt、审查规则），Tool 负责真实执行（DashScope 生图）。本站原创，根据 openclaw-blog-agent 实际实现整理。</p>
<p>Tool 解决了”能不能做”，但没解决”该怎么做、按什么顺序、受什么约束”。当任务有一类稳定的流程——比如”写一篇 AI 科技日报”或”为文章生成配图”——需要把规则、步骤、模板和调用条件固化下来，这就是 Skill。</p>
<p>Skill 更接近<strong>任务说明书</strong>：它描述目标、步骤、边界和判断标准，本身不一定执行真实动作。Skill 通常包含：</p>
<ul>
<li>研究策略与来源优先级；</li>
<li>写作模板与风格要求；</li>
<li>安全边界与禁止行为；</li>
<li>Tool 的调用条件（什么时候调、参数怎么填）。</li>
</ul>
<p>因此，<strong>没有 Tool 时，Skill 可能只能生成规划或 Prompt</strong>——这一点在本地实现里看得很清楚。<code>academic-figure-prompt</code> 这个 Skill 负责：分析文章、生成 Figure Spec（节点/连线/层次）、选择配色、给出比例与图注建议、生成英文生图 Prompt 和审查清单。它<strong>不调用任何生图接口</strong>；真正的图片生成由 Tool <code>generate_academic_figure</code> 完成。Skill 与 Tool 的分工是：<strong>Skill 决定”画什么、按什么规范画”，Tool 决定”真的画出来”</strong>。</p>
<p>同样要注意：不同系统的 Skill 机制并不相同。本仓库的 Skill 是”SKILL.md 规则文件 + 由 Agent 读取执行”；其他平台可能有完全不同的目录结构和加载方式。把某一家的 Skill 当成通用标准，是常见误解。</p>
<p>还需要区分 Skill 与 MCP 的 prompts：MCP 规范里的 prompts 是服务器侧暴露的”模板化消息与工作流”，本质是一个可发现、可检索、可传参的协议对象；而本文讨论的 Skill 是客户端本地组织任务规则的文档。两者名字相近、定位不同——一个属于协议层，一个属于编排层。</p>
<p>Skill 的另一个特征是<strong>可组合性</strong>：一个 Skill 可以调用另一个 Skill 的结果。本仓库里 <code>jaisong1n-blog</code> 负责识别任务类型并决定调用哪个 Skill，<code>academic-figure-prompt</code> 负责配图规划，<code>blog-news-research</code> 负责研究——它们各管一段，通过 Agent 串联成完整流程。这种”任务分解 + 各自为政”的组织方式，正是 Skill 与”把所有逻辑写进一个 Tool”的差别所在。</p>
<p>另一个诚实的边界：当前博客 Agent 的 Skill 只能做规划和 Prompt，<strong>WordPress 媒体上传并未实现</strong>——所以”生成图片并插入文章”目前止步于”生成本地图片 + 返回路径”，这一点不能因为文章叙事而写成已实现。</p>
<h2>MCP：解决外部能力如何标准连接</h2>
<p><img src="https://cms.jaisong1n.com/wp-content/uploads/2026/08/mcp-simple-diagram-scaled.png" alt="MCP 的 Client/Server 架构与 Tools、Resources、Prompts 三类能力" /></p>
<p>MCP 的 Client/Server 结构：Host（AI 应用）通过标准协议连接 Server，Server 暴露 Tools、Resources、Prompts 三类能力。来源：Model Context Protocol project，许可 CC BY 4.0，未修改。</p>
<p>当你要接的不是一个 API，而是一整类外部系统（数据库、GitHub、搜索服务）时，逐个写适配代码会变得昂贵。MCP（Model Context Protocol）的定位是：<strong>一个开放的、标准化的连接协议</strong>，让 AI 应用通过统一方式连接外部数据源与工具。官方文档把它类比为”AI 应用的 USB-C 接口”。</p>
<p>MCP 的核心结构是 <strong>Client 与 Server</strong>：</p>
<ul>
<li>Client 是 AI 应用（如 Claude、ChatGPT、IDE）；</li>
<li>Server 暴露能力，能力分为三类（官方规范定义）：<strong>tools</strong>（模型可执行的函数）、<strong>resources</strong>（提供上下文的数据，按 URI 标识）、<strong>prompts</strong>（可复用的模板化消息与工作流）。</li>
</ul>
<p>需要特别说清 MCP 的三条边界：</p>
<ol>
<li><strong>MCP 解决的是连接与互操作</strong>，不是替 Agent 设计工作流。Server 暴露 tools/resources/prompts，但”任务该怎么编排、何时调用、结果怎么审查”仍然由客户端与上层 Agent 逻辑决定。</li>
<li><strong>MCP Server 暴露 Tool，不代表 Tool 只能通过 MCP 提供</strong>。本地直接注册的 Tool（如当前 OpenClaw 的 <code>generate_academic_figure</code>）完全合法且常见。</li>
<li><strong>MCP 不自动提供业务安全、确认机制与内容质量规则</strong>。协议规范本身强调实现方必须校验输入输出、防止注入与未授权访问——也就是说，安全策略在协议之外，靠具体实现。</li>
</ol>
<p>因此，把 MCP 描述成”万能 Agent 框架”是不准确的：它是一层连接标准，不是任务编排层，也不是安全层。</p>
<h2>三者如何在一个 Agent 系统里协作</h2>
<p>以当前博客 Agent 的真实链路为例。用户提出：”给这篇文章生成一张技术配图。”</p>
<ol>
<li><code>jaisong1n-blog</code> Skill 识别这是文章配图任务，决定调用配图 Skill；</li>
<li><code>academic-figure-prompt</code> Skill 分析内容，生成 Figure Spec（节点、连线、层次）、配色与比例建议、正式文字标签清单，以及最终英文生图 Prompt；</li>
<li><code>generate_academic_figure</code> Tool 接收 Prompt，校验参数，调用 DashScope 百炼（qwen-image-2.0），下载图片并保存到本地生成目录；</li>
<li>通过 OpenClaw 微信通道把本地图片返回给用户（message 工具携带本地媒体路径）；</li>
<li>后续如接入 Site Manager 媒体接口，再由它负责上传与插入正文——<strong>目前尚未实现</strong>；</li>
<li>未来某些外部能力（如第三方图片素材库、内容审核服务）也可以通过 MCP Server 提供，但当前没有接入。</li>
</ol>
<p>值得强调：<strong>当前 OpenClaw 的图片工具是本地插件 Tool，不是通过 MCP 提供的</strong>。三层（Skill 编排 → 本地 Tool 执行 → 未来 MCP 标准接入）在同一条链路上各司其职。</p>
<h2>最容易混淆的五个问题</h2>
<p><strong>1. Skill 是不是更复杂的 Tool？</strong><br />
不是。Tool 是执行单元，Skill 是任务组织单元。一个 Skill 可以调用多个 Tool，也可以一个都不调（只出规划）。复杂度不是区分标准，层次才是。</p>
<p><strong>2. MCP Server 是不是一个 Skill？</strong><br />
不是。MCP Server 暴露的是可发现、可调用的能力（tools/resources/prompts）；Skill 是客户端一侧的任务规则。两者可能都涉及”prompt”，但一个是协议对象，一个是本地编排文档。</p>
<p><strong>3. 有了 MCP 还需要本地 Tool 吗？</strong><br />
需要。MCP 是连接标准，不是执行引擎。本地 Tool 承担客户端自身业务逻辑（权限校验、确认流程、内容规则），这些恰好是 MCP 协议不覆盖的部分。</p>
<p><strong>4. Skill 能不能不包含代码？</strong><br />
能。本仓库的 Skill 就是纯 Markdown 规则文件（SKILL.md + references），不含可执行代码；”执行”交给 Tool。Skill 定义策略，Tool 提供能力。</p>
<p><strong>5. Tool、Skill 和 Agent 的关系是什么？</strong><br />
Agent 是使用工具、遵循 Skill、与用户交互的主体；Tool 是它的”手”，Skill 是它的”操作手册”。MCP 则是”手”可以通过标准接口触碰外部设备的方式之一。</p>
<h2>我对这三层设计的判断</h2>
<p>基于当前项目的实际结构，我的判断是：</p>
<ul>
<li><strong>把业务规范全塞进 Tool 会导致工具过重</strong>。Tool 的参数和执行逻辑应该保持原子；”什么时候用、按什么标准”应该放 Skill。<code>generate_academic_figure</code> 只负责”调 API、保存、返回路径”，生图规范全在 <code>academic-figure-prompt</code> 里——这层分离让两边都能独立演进。</li>
<li><strong>只写 Skill 而没有 Tool，最终只能停留在规划层</strong>。Skill 出得了 Figure Spec 和 Prompt，但没 Tool 就永远没有 PNG。规划与执行缺一不可。</li>
<li><strong>MCP 适合解决外部能力接入，但不能代替应用内部的审批与安全策略</strong>。MCP 规范自身也强调实现方要自己做输入输出校验与访问控制——协议给了连接，没给信任。</li>
<li><strong>对个人博客 Agent 来说，本地 Tool + Skill 已经能解决很多问题</strong>，不必为了”用了 MCP”而强行引入。当某类能力需要被多个客户端复用（比如同一生图服务同时给 OpenClaw、Claude Code 和其他应用用）时，MCP 的价值才会更明显。</li>
</ul>
<p>还有一个容易被忽视的现实：<strong>“Skill”这个词在不同工具链里含义漂移严重</strong>。在 Claude Code 里，Skill 是 <code>~/.claude/skills</code> 下的目录与 SKILL.md；在 OpenClaw 里，Skill 是 <code>workspace/skills</code> 下的规则文件；在 MCP 规范里根本没有”Skill”这个概念，只有 prompts。这意味着你读到”Skill”时，必须先确认对方指的是哪个平台、哪一层。本文所有关于 Skill 的表述，都限定在 OpenClaw 这类”SKILL.md 规则文件”的实现语境里，不推广成普适定义。</p>
<p>以上是个人判断，基于当前项目可验证的事实；没有做过的性能测试、尚不存在的 MCP 集成，我不作任何”已实现”或”最佳实践”的声称。</p>
<h2>总结</h2>
<p>读完这篇文章，你可以带走三个结论：</p>
<ol>
<li><strong>Tool、Skill、MCP 不互相替代</strong>——它们分别是执行层、编排层和连接层的产物，可以共存于同一个 Agent。</li>
<li><strong>判断一个名词，先看它解决什么问题</strong>：执行动作（Tool）、稳定流程（Skill）、标准连接（MCP）。三者混为一谈的根源，是只盯着”都是给模型加能力”这个表象。</li>
<li><strong>对个人 Agent 而言，本地 Tool + Skill 是主力，MCP 是可选的标准通道</strong>：先让业务闭环跑起来，再在能力需要跨客户端复用时考虑 MCP。</li>
</ol>
<h2>资料来源</h2>
<p>以下为实际读取并支撑本文的官方资料与本地实现：</p>
<ul>
<li>Model Context Protocol 官方规范（2025-06-18 / 2026-07-28）：https://modelcontextprotocol.io/specification/ — 定义 server features（tools/resources/prompts）、Client/Server 结构与消息格式</li>
<li>MCP 官方介绍页：https://modelcontextprotocol.io/introduction — “USB-C for AI applications” 定位</li>
<li>MCP 规范 Server Features 页（Resources / Prompts / Tools）：https://modelcontextprotocol.io/specification/2025-06-18/server/（tools、resources、prompts 分页）</li>
<li>Anthropic Claude Platform Docs — Tool use with Claude：https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview — tools 参数、tool_choice、tool_use/tool_result 消息</li>
<li>OpenAI API 文档 — Function calling：https://platform.openai.com/docs/guides/function-calling — function tool 的 name/description/parameters(JSON Schema)/strict 定义</li>
<li>本地 OpenClaw 实现：<code>plugins/jaisong1n-blog-tools/src/index.ts</code> 与 <code>openclaw.plugin.json</code> — 本地 Tool 注册（<code>generate_academic_figure</code>、<code>edit_academic_figure</code> 等 13 个工具）</li>
<li>本地 Skill：<code>skills/academic-figure-prompt/SKILL.md</code>、<code>skills/jaisong1n-blog/SKILL.md</code> — Skill 与 Tool 的职责边界、规划/生成/修改调度规则</li>
</ul>
<p><em>注：本文中的”Skill”指本仓库这类”SKILL.md 规则文件”实现；不同平台对 Skill 的实现与加载方式可能不同，MCP 与 Anthropic/OpenAI 的官方定义均不包含统一的”Skill”标准。</em></p>
]]></content>
    <author>
      <name>JaisonG1n</name>
    </author>
    <category term="Uncategorized"></category>
  </entry>
  <entry>
    <title>从手动发布到自动部署：我的个人博客工作流升级记录</title>
    <link href="https://jaisong1n.com/posts/wordpress-astro-auto-deploy-workflow/" rel="alternate" type="text/html"/>
    <id>https://jaisong1n.com/posts/wordpress-astro-auto-deploy-workflow/</id>
    <published>2026-07-31T07:41:50.000Z</published>
    <updated>2026-07-31T07:41:50.000Z</updated>
    <summary>记录个人博客从手动触发构建升级为 WordPress 发布后自动调用 GitHub Actions，并由 Astro 完成同步、构建和部署的过程。</summary>
    <content type="html"><![CDATA[<h2>为什么要改造发布流程</h2>
<p>过去我在 WordPress 中完成一篇文章的修改后，还要专门切换到构建页面手动触发流程。这个动作本身并不复杂，但很容易在赶时间时被遗漏：内容已经保存，线上页面却仍然是旧版本。写作和发布被拆成两段，注意力也被迫从文章本身转移到一次次确认构建状态上。随着文章更新越来越频繁，我希望把这段重复操作交给稳定的自动化链路，让自己把时间留给选题、表达和审核。</p>
<h2>现在的发布链路</h2>
<p>现在，当 WordPress 发布内容后，系统会先经过约 60 秒的防抖窗口，把短时间内连续的修改合并起来，再通过 workflow_dispatch 触发 GitHub Actions。随后工作流同步 WordPress 的结构化数据和媒体资源，执行 Astro 静态构建，更新 deploy 分支，最后让博客上线。这样的顺序让内容源始终清晰，也避免了每次微调都触发一次完整构建。</p>
<ul>
<li><code>WordPress 发布内容</code></li>
<li><code>约 60 秒防抖并合并连续修改</code></li>
<li><code>workflow_dispatch</code></li>
<li><code>GitHub Actions</code></li>
<li><code>同步数据和媒体</code></li>
<li><code>Astro 静态构建并更新 deploy 分支</code></li>
</ul>
<h2>这次改造中关注的细节</h2>
<p>我把可追溯性和失败隔离放在第一位。Site Manager 插件版本为 0.6.0，schemaVersion 保持为 5，保证已有内容结构可以继续被稳定识别。用于调用 GitHub 的 Token 采用最小权限，并且不会进入前端产物、内容快照或运行日志。连续保存会在防抖窗口内合并，不会产生多次构建；即使 GitHub 请求失败，也不会阻止 WordPress 保存内容。手动强制重建仍然保留为兜底入口。新插件统一使用 workflow_dispatch，repository_dispatch 暂时只保留给旧调用方兼容使用。</p>
<h2>当前完成情况</h2>
<p>目前，手动强制构建已经成功，自动触发构建也已经跑通。这篇文章用于继续验证 AI 通过 REST API 管理 WordPress 内容的能力。内容写入阶段会保持草稿状态，由我先检查结构、措辞和事实，再决定是否发布；这也让自动化的边界始终清楚。</p>
<h2>下一步</h2>
<p>接下来我会开发受控的 AI 草稿接口，并接入 OpenClaw 微信助手。AI 默认只创建草稿，人工审核后再发布；一旦内容发布，就复用现有的自动构建链路完成同步、构建和部署。这样既能提升写作效率，也能保留必要的人工判断。</p>
]]></content>
    <author>
      <name>JaisonG1n</name>
    </author>
    <category term="Uncategorized"></category>
  </entry>
  <entry>
    <title>JaisonG1n 博客测试文章</title>
    <link href="https://jaisong1n.com/posts/jaisong1n-%E5%8D%9A%E5%AE%A2%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/" rel="alternate" type="text/html"/>
    <id>https://jaisong1n.com/posts/jaisong1n-%E5%8D%9A%E5%AE%A2%E6%B5%8B%E8%AF%95%E6%96%87%E7%AB%A0/</id>
    <published>2026-07-28T14:46:27.000Z</published>
    <updated>2026-07-28T14:46:28.000Z</updated>
    <summary>这是一篇用于测试 WordPress 与 Mizuki 数据同步的文章。</summary>
    <content type="html"><![CDATA[<p>这是一篇用于测试 WordPress 与 Mizuki 数据同步的文章。</p>
]]></content>
    <author>
      <name>JaisonG1n</name>
    </author>
    <category term="开发笔记"></category>
  </entry>
</feed>